import 'package:flutter/material.dart';

class AdminRepairCompaniesTab extends StatelessWidget {
  const AdminRepairCompaniesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('$this'),
    );
  }
}
