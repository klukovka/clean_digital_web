import 'package:flutter/material.dart';

class AdminBackupsTab extends StatelessWidget {
  const AdminBackupsTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('$this'),
    );
  }
}
