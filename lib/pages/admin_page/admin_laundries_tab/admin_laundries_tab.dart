import 'package:flutter/material.dart';

class AdminLaundriesTab extends StatelessWidget {
  const AdminLaundriesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('$this'),
    );
  }
}
